<div class="container">
    <div class="header__box">
        {{#with header}}
            <h1 class="header__title">{{headerH1}}</h1>
            <a href="{{btnLink}}" class="btn">{{btnName}}</a>
        {{/with}}
    </div>
</div>

